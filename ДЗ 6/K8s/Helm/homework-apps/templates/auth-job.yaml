{{- if and .Values.authService.enabled .Values.authService.migrationJob.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "homework-apps.authFullname" . }}-migration
  labels:
    {{- include "homework-apps.authLabels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  ttlSecondsAfterFinished: {{ .Values.authService.migrationJob.ttlSecondsAfterFinished }}
  completions: 1
  backoffLimit: {{ .Values.authService.migrationJob.backoffLimit }}
  template:
    spec:
      containers:
        - name: {{ .Values.authService.name }}-migration
          image: {{ .Values.authService.migrationJob.imageRepository }}:{{ .Values.authService.migrationJob.imageTag }}
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: {{ include "homework-apps.authFullname" . }}
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ include "homework-apps.authFullname" . }}
                  key: DB_PORT
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: {{ include "homework-apps.authFullname" . }}
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "homework-apps.authFullname" . }}
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "homework-apps.authFullname" . }}
                  key: DB_PASSWORD
      restartPolicy: Never
{{- end }}
