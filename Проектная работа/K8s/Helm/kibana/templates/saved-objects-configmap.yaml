apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-saved-objects
  labels:
    app.kubernetes.io/name: kibana
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  electronics-store.ndjson: |
{{ .Files.Get "files/electronics-store.ndjson" | indent 4 }}
