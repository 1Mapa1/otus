{{- if .Values.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "catalog-service.fullname" . }}
  labels:
    {{- include "catalog-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-20"
type: {{ .Values.global.secretType | default "Opaque" }}
stringData:
  DB_PRIMARY_USER: {{ .Values.secret.dbUser }}
  DB_PRIMARY_PASSWORD: {{ .Values.secret.dbPassword }}
  DB_REPLICA_USER: {{ .Values.secret.dbUser }}
  DB_REPLICA_PASSWORD: {{ .Values.secret.dbPassword }}
{{- end }}
