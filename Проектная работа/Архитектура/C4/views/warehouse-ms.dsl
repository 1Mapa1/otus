component onlineStore.warehouseMs "WarehouseMsComponents" {
    title "C3. Компоненты — WarehouseMs"
    description "Внутренняя архитектура WarehouseMs: остатки, резервы, pessimistic locking, product lifecycle events и outbox."

    include *?

    autoLayout lr
}