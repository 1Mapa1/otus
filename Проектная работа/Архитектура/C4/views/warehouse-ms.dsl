component onlineStore.warehouseMs "WarehouseMsComponents" {
    title "C3. Компоненты — WarehouseMs"
    description "Целевая внутренняя архитектура WarehouseMs: остатки, резервы, optimistic locking, product lifecycle events, reservation TTL и outbox."

    include *?

    autoLayout lr
}