component onlineStore.catalogMs "CatalogMsComponents" {
    title "C3. Компоненты — CatalogMs"
    description "Целевая внутренняя архитектура CatalogMs: CQRS-lite, Cache-Aside, Redis, Read Replica, Kafka consumer и outbox."

    include *?

    autoLayout lr
}